import { ItemsStateInterface } from '@/models/store';
/**
 * @name initialItemsState
 * @description
 * Instance of Items state with default values
 */

export const initialItemsState: ItemsStateInterface = {
  loading: false,
  items: [],
};
